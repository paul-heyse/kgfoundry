# Enable OpenTelemetry tracing/metrics for the MCP server (0 disables).
CODEINTEL_OTEL_ENABLED=1

# Logical service identity reported to collectors.
CODEINTEL_OTEL_SERVICE_NAME=codeintel-mcp

# Primary OTLP HTTP endpoint for traces (and optionally metrics/logs).
# Include the full path segment expected by your collector, e.g.:
#   http://localhost:4318/v1/traces
CODEINTEL_OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318/v1/traces

# Optional metrics endpoint (falls back to CODEINTEL_OTEL_EXPORTER_OTLP_ENDPOINT).
# CODEINTEL_OTEL_METRICS_ENDPOINT=http://localhost:4318/v1/metrics

# Sampler configuration — parentbased_traceidratio keeps parents when present
# and samples new traces at the provided ratio (0.0–1.0).
CODEINTEL_OTEL_SAMPLER=parentbased_traceidratio:0.25

# Toggle OpenTelemetry logging + metric instrumentation hooks.
CODEINTEL_OTEL_LOGS_ENABLED=1
CODEINTEL_OTEL_METRICS_ENABLED=1

# Optional: customize diagnostic output directories used by the flight recorder.
DATA_DIR=./data
CODEINTEL_DIAG_DIR=./data/diagnostics
